version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env          # contains DATABASE_URL pointing to AWS RDS
    ports:
      - "8000:8000"
    restart: unless-stopped
